# Карта файлов

Полное дерево репозитория. Всё, чего нет в этом списке, в репозитории быть не должно.

```
test_trash/
│
├── CLAUDE.md                      регламент работы
├── README.md                      описание проекта и запуск
├── .gitignore                     что не коммитим
├── robots.txt                     запрет индексации
├── manifest.webmanifest           установка на главный экран
│
├── index.html                     проверка контура доставки
├── app.html                       приложение
├── check.html                     самопроверка ядра, 20 проверок
├── states-water.html              витрина состояний кружка
├── states-card.html               витрина карточки
│
├── assets/
│   ├── css/
│   │   ├── tokens.css             цвета, шрифты, отступы, волосяная линия
│   │   ├── base.css               сброс, типографика, список проверок
│   │   ├── card.css               карточка темы, график, точки-указатели
│   │   ├── home.css               зоны экрана, меню, таблетка, панели, лента
│   │   └── preview.css            только служебные витрины
│   └── icons/
│       ├── logo.svg               исходник логотипа
│       ├── icon-180.png           ярлык на главный экран iPhone
│       ├── icon-192.png           значок вкладки
│       └── icon-512.png           крупный значок для манифеста
│
├── src/
│   ├── app.js                     сборка приложения на живых данных
│   ├── main.js                    диагностика для index.html
│   │
│   ├── core/                      ядро: данные и ничего кроме
│   │   ├── schema.js              таблицы, значения по умолчанию, проверка записей
│   │   ├── store.js               единая точка доступа, подписки, выгрузка
│   │   ├── adapter-local.js       хранение в браузере
│   │   ├── migrate.js             переход между версиями схемы
│   │   ├── backup.js              выгрузка и загрузка файлом
│   │   └── check.js               двадцать проверок ядра
│   │
│   ├── modules/                   темы: логика предметной области
│   │   └── water.js               «Водный баланс»: записи, норма, срочность
│   │
│   └── ui/                        интерфейс: рисует, но не решает
│       ├── circle.js              кружок темы, все состояния
│       ├── card.js                карточка темы и блок графика
│       ├── chart.js               линии и столбики
│       ├── gestures.js            тап, удержание, листание
│       ├── undo.js                таблетка отмены и стопка
│       ├── menu.js                меню: норма, данные, опасная зона
│       ├── recent.js              лента недавних записей за сутки
│       ├── water-screen.js        развёрнутый экран воды
│       ├── states-water.js        витрина состояний кружка
│       └── states-card.js         витрина карточки
│
└── docs/
    ├── files.md                   этот файл
    ├── modules.md                 карта модулей и волны сборки
    ├── data-model.md              таблицы, хранилище, интерфейс доступа
    ├── contracts.md               контракты оболочки и тем
    ├── module-water.md            макет темы воды
    ├── postgres-route.md          маршрут перехода на свой сервер
    └── decisions.md               журнал решений
```

## Три слоя и правило зависимостей

Зависимости идут только вниз. Нарушение этого правила — повод остановиться, а не обойти.

| Слой | Что можно | Чего нельзя |
|------|-----------|-------------|
| `core` | Знать про данные | Знать про воду, кружки и экраны |
| `modules` | Знать про свою предметную область и обращаться к `core` | Знать про другие темы и про разметку |
| `ui` | Рисовать по переданным данным | Читать хранилище напрямую |
| `app.js` | Связывать всё вместе | Содержать предметную логику |

Проверка: если ради новой темы приходится править `ui` или `core` — контракт составлен неверно.

## Что не коммитить

Файлы `preview-*.png` — снимки для переписки. В репозиторий не идут.

## Проверка после раскладки

```bash
git status          # не должно быть preview-, .gitkeep, home.html
python3 -m http.server 8000
```

Открыть по очереди: `/` — контур, `/check.html` — ядро, `/app.html` — приложение.
Пустая страница означает, что файл лежит не в той папке. Консоль браузера покажет, какого не хватает.
